#include <iostream>

int main() {
  int mi_variable;
  // bool otra_var;
  // otra_var = (mi_variable = 0);
  // std::cout << static_cast<int>('a') << std::endl;
  // std::cout << mi_variable << std::endl;
  // std::cout << otra_var << std::endl;
  //
  while (std::cin >> mi_variable) {
    std::cout << "The value entered was " << mi_variable << std::endl;
  } 
  return 0;
  
}
