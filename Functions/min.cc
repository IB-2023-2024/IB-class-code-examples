
#include <iostream>

int Min(int x, int y) {
  // if (x < y)
  //   return x;
  // else
  //   return y; 
  // int my_var{x};
  // return x < y ? x : y;
  return;
}

int main () {

  std::cout << Min(3, 4) << std::endl;
}
